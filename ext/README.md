# ext/velib_python

`setup install` clones [velib_python](https://github.com/victronenergy/velib_python)
here at install time. It is git-ignored on purpose so the package itself stays
small and tracks upstream.
